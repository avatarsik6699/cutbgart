{
  "$comment": "Declares the pinned Hugging Face model revision and ONNX Runtime Web version synchronized to the VPS directory served at cdn.cutbg.art/models (SPEC.md §6, §6.1). Binaries are never committed. The immutable revision makes the CDN path safe for long-lived caching.",
  "models": [
    {
      "id": "onnx-community/ISNet-ONNX",
      "revision": "3fe6e3db3e32c69aadde61fe388ddb1a0574440c",
      "files": [
        "config.json",
        "preprocessor_config.json",
        "onnx/model.onnx",
        "onnx/model_quantized.onnx"
      ]
    }
  ],
  "onnxRuntimeWeb": {
    "$comment": "Must match package.json. The sync script copies the installed package's full dist/ directory because the browser selects the required runtime variant.",
    "version": "1.27.0"
  }
}
